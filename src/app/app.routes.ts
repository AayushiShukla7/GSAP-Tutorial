import { Routes } from '@angular/router';
import { ScrollTriggerComponent } from './scroll-trigger/scroll-trigger.component';

export const routes: Routes = [
    {
        path: 'scroll-trigger',
        component: ScrollTriggerComponent
    }
];
